[pytest]
testpaths = automation/tests
pythonpath = .

markers =
    smoke: Critical path tests — login, products, cart, checkout
    regression: Full regression suite covering all application modules

addopts =
    -v
    --html=reports/report.html
    --self-contained-html
